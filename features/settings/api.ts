import { requestJson } from '@/lib/api'; import type { ActiveLocale,ThemePreference } from '@/localization';
type Preferences={preferredLocale:string|null;localeSource:'system'|'manual';theme:ThemePreference;hideLastName:boolean};
export async function getPreferences(token:string){const r=await requestJson<{success:boolean;preferences:null|{preferred_locale:string|null;locale_source:'system'|'manual';theme:ThemePreference;hide_last_name:boolean}}>('/api/settings/preferences',{headers:{Authorization:`Bearer ${token}`}}); return r.preferences?{preferredLocale:r.preferences.preferred_locale,localeSource:r.preferences.locale_source,theme:r.preferences.theme,hideLastName:r.preferences.hide_last_name}:null;}
export async function savePreferences(token:string,p:{locale:ActiveLocale;theme:ThemePreference;hideLastName:boolean}){await requestJson('/api/settings/preferences',{method:'PUT',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({preferredLocale:p.locale,theme:p.theme,hideLastName:p.hideLastName})});}
